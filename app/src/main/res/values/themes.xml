<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SoundSphere" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Status bar -->
        <item name="android:statusBarColor">#000000</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Navigation bar (system buttons) -->
        <item name="android:navigationBarColor">#000000</item>

        <!-- Optional: Enable drawing behind system bars -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <style name="Theme.SoundSphere" parent="Base.Theme.SoundSphere" />

    <style name="ShapeAppearance.App.CornerSize20dp" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">20dp</item>
    </style>

</resources>
